services:
  - type: web
    name: movie-laravel
    env: docker
    plan: free
    branch: main
    region: frankfurt
    dockerfilePath: ./Dockerfile
    buildCommand: ""
    startCommand: php artisan serve --host 0.0.0.0 --port 10000
